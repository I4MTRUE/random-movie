function pad(number, length) {
  var str = '' + number;
  while(str.length < length) {
    str = '0' + str;
  }
 return str;
}
function search{
  var title = 1;
  var year = 2;
  $("#title").text(title);
  $("#year").text(year);
return [title, year, rating, actors, plot, season, imdbVotes]
}
